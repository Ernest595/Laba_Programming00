#include <stdio.h>
#include <string.h>
#include <ctype.h>

/*
void print_matrix(
    int const * const *matrix,
    size_t rows_count,
    size_t columns_count)
{
    int i, j;
    for (i = 0; i < rows_count; ++i)
    {
        for (j = 0; j < columns_count; ++j)
        {
            printf("%d ", matrix[i][j]);
        }
        printf("\n");
    }
}

int multiply_matrices(
    int const* const* matrix1,
    size_t rows_count1,
    size_t columns_count1,
    int const* const* matrix2,
    size_t rows_count2,
    size_t columns_count2,
    int ***result,
    size_t *result_rows_count,
    size_t *result_columns_count)
{
    int i, j, k;

    if (columns_count1 != rows_count2)
    {
        return 1;
    }

    int** matrix;
    double** multiplicate_matrix(double** matrix1, double** matrix2, int rows1,
        int cols1, int cols2)
    {
        double** new_matrix = (double**)malloc(rows1 * sizeof(double*));
        for (int i = 0; i < rows1; i++) {
            new_matrix[i] = (double*)malloc(cols2 * sizeof(double));
        }

    }

    
        
        for (i = 0; i < rows_count1; ++i)
    {
        for (j = 0; j < columns_count1; ++j)
        {
            matrix[i, j] = 0;
            for (k = 0; k < columns_count2; ++k)
            {
                matrix[i, j] += matrix1[i][k] * matrix2[k][j];
            }
        }
    }

    *result = matrix;
    *result_rows_count = rows_count1;
    *result_columns_count = columns_count2;

    return 0;
}

*/



    
    





}























