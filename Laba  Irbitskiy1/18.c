#include <stdio.h>
#include <stdlib.h>
#include<ctype.h>
#include <string.h>

/*
#define SIZE 5


void transpond_matrix(int matrix[SIZE][SIZE]) {
	int i, j, t;
	for (i = 0; i < SIZE; i++) {

		for (j = i + 1; j < SIZE; j++) {

			t = matrix[i][j]
			matrix[i][j] = matrix[j][i];
			matrix[j][i] = t;
		}
	}
}

void print_matrix(int matrix[SIZE][SIZE]) {

	int i, j;
	for (i = 0; i < SIZE; ++i)
	{
		for (j = 0; j < SIZE; ++j)
		{
			printf("%d ", matrix[i][j]);
		}
		printf("\n");
	}
}

int main() {

	int matrix[SIZE][SIZE] = { {34, 0, 8, 6, 10},
							   {5, 8, 11, 19, },
							   {6, 78, 10, 18, 14},
							   {9, 6, 82, 4, 24},
								{55, 49, 43, 189, 100} };

	printf("Original matrix = \n");
	print_matrix(matrix);
	puts("\n");
	transpond_matrix(matrix);
	printf("Transpond_matrix\n");
	print_matrix(matrix);
	return 0;
*/
























}

