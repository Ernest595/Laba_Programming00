#include <stdio.h>
#include <string.h>
#include <ctype.h>
/*
int main()
{
    char string[10] = "helloworld";
    char* p = string;
    int i;

    for (i = 0; i < strlen(string); ++i)
    {
        if (i % 2 != 0)
        {
            *p = toupper(*p);
        }

        printf("%c", *p++);
    }

    return 0;
}


*/








