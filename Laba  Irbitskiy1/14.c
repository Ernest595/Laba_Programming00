#include <stdio.h>
#include <string.h>
#include <stdlib.h>
/*

#define ARRAY_SIZE 10000


int programm() {

	FILE* fptr = fopen("1.txt", "r");

	if (fptr = NULL) {
		return 1;
	}

	int* array = (int*)malloc(sizeof(int) * ARRAY_SIZE);
	read_array_from_file(fptr, array);
	print_array(array, ARRAY_SIZE);
	int an[4];
	find_max(array, ARRAY_SIZE, an);
	printf("Max element: %d, index: %d\nMin element: %d, index: %d\n", an[0],
		an[1], an[2], an[3]);

	free(array);
	fclose(fptr);
	return 0;
}
*/



	