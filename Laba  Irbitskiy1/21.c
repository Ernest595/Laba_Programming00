#define _CRT_SECURE_NO_WARNINGS

/*

//Задание 21

#include<stdio.h>
#include<ctype.h>


int gorner(const char* str, int base) {
    int result = 0;
    char const* s = str;
	while (*s)
	{
        result = result * base + (isdigit(*s) ? *s - '0' : tolower(*s) - 'a' + 10);
        printf("result after iter #%d: %d\n", s - str, result);
        s++;
	}
    return result;
		
}

int main(void) {
    char* s[15];
    int b;
    scanf("%s", s);
    scanf("%d", &b);
    printf("%d", gorner(s, b));

    return 0;

}

*/















