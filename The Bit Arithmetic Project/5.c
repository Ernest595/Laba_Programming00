#include <stdio.h>
/*
int swap(int n, int i, int j);


int main() {
    int n, i, j;
    // ¬вод числа и индексов битов
    printf("¬ведите число n: ");
    scanf("%d", &n);
    printf("¬ведите индекс i: ");
    scanf("%d", &i);
    printf("¬ведите индекс j: ");
    scanf("%d", &j);

    printf("New number: %d\n", swap(n, i, j));

    return 0;
}


int swap(int n, int i, int j) {
    int i_bit = n & (1 << i);
    int j_bit = n & (1 << j);
    if (i_bit != j_bit) {
        n = n ^ (1 << i);
        n = n ^ (1 << j);
    }

    return n;
}
*/