#include <stdio.h>


/*
int xor_all_digits(int n);

int main() {
    int n;
    scanf("%d", &n);

    int an = xor_all_digits(n);
    printf("XOR all digits result: %d\n", an);
    return 0;
}


int xor_all_digits(int n) {
    int an = n % 2;
    n /= 2;
    while (n > 0) {
        an = an ^ (n % 2);
        n /= 2;
    }
    return an;
}

*/
